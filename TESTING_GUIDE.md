# 测试指南

## 快速测试

### 1. 运行自动化测试

打开 `test.html` 文件，它会自动运行基础功能测试。

### 2. 运行开发模式测试

```bash
npm run tauri:dev
```

这将启动开发模式，你可以：
- 测试所有功能
- 查看控制台日志
- 实时查看代码更改

### 3. 测试打包后的应用

```bash
npm run tauri:build
```

然后运行生成的安装包：
- Windows: `src-tauri\target\release\bundle\nsis\Minimal Pomodoro Timer_1.0.1_x64-setup.exe`

## 详细测试步骤

### 测试 1: 应用启动
1. 启动应用
2. 检查窗口大小是否为 500x600
3. 检查窗口是否可以调整大小
4. 检查所有 UI 元素是否可见

### 测试 2: 计时器功能
1. 点击"Start"按钮
2. 选择"Start Pomodoro"
3. 观察计时器是否开始倒计时
4. 点击"Pause"暂停
5. 点击"Start"恢复
6. 点击"Reset"重置

### 测试 3: 设置功能
1. 点击设置按钮（⚙️）
2. 修改番茄钟时长为 30 分钟
3. 点击"Save"
4. 重新打开设置，检查是否保存成功
5. 修改设置后点击"Cancel"，检查是否未保存

### 测试 4: 通知功能
1. 在设置中启用通知
2. 启动一个很短的计时器（如 1 分钟）
3. 等待计时器完成
4. 检查是否显示通知
5. 检查通知内容是否正确

### 测试 5: 状态恢复
1. 启动计时器
2. 关闭应用
3. 重新打开应用
4. 检查计时器状态是否恢复
5. 检查时间是否正确

### 测试 6: 阶段切换
1. 完成一个番茄钟
2. 检查是否自动切换到小休
3. 完成小休
4. 检查是否自动切换回番茄钟
5. 完成 4 个番茄钟
6. 检查是否切换到大休

## 常见问题排查

### 问题 1: 应用无法启动
- 检查 Rust 是否正确安装
- 检查 Node.js 版本是否 >= 18
- 查看控制台错误信息

### 问题 2: 计时器不工作
- 打开浏览器控制台（F12）
- 检查是否有 JavaScript 错误
- 检查 DesktopTimer 是否正确初始化

### 问题 3: 设置无法保存
- 检查 localStorage 是否可用
- 检查浏览器控制台是否有错误
- 检查 Storage.js 是否正确加载

### 问题 4: 通知不显示
- 检查通知权限是否已授予
- 检查通知图标路径是否正确
- 检查浏览器控制台是否有错误

## 性能测试

### 内存使用
1. 打开任务管理器
2. 启动应用
3. 观察内存使用（应该 < 100MB）
4. 运行应用 1 小时
5. 检查是否有内存泄漏

### CPU 使用
1. 打开任务管理器
2. 启动应用
3. 观察 CPU 使用（应该 < 5%）
4. 运行计时器
5. 检查 CPU 使用是否正常

## 测试报告模板

使用 `TEST_CHECKLIST.md` 记录测试结果。

## 自动化测试

`test.html` 提供了基础的自动化测试，包括：
- 存储功能测试
- 计时器对象测试
- 设置功能测试
- UI 元素测试

## 下一步

完成所有测试后，填写 `TEST_REPORT.md` 记录测试结果和发现的问题。

