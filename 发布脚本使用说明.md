# 发布脚本使用说明

## 快速开始

### 基本用法

```powershell
# 自动从 manifest.json 读取版本号，打包并发布
.\release.ps1

# 指定版本号发布
.\release.ps1 -Version "1.0.1"

# 指定版本号和提交消息
.\release.ps1 -Version "1.0.1" -Message "feat: 添加新功能"

# 包含发布说明
.\release.ps1 -Version "1.0.1" -ReleaseNotes "## 更新内容`n- 新功能1`n- Bug修复1"

# 创建GitHub Release（需要安装GitHub CLI）
.\release.ps1 -CreateRelease

# 仅本地操作，不推送到GitHub
.\release.ps1 -SkipPush

# 跳过打包步骤（仅Git操作）
.\release.ps1 -SkipBuild
```

## 参数说明

### -Version
- **类型**: String
- **默认**: 从 manifest.json 自动读取
- **说明**: 指定发布版本号
- **示例**: `-Version "1.0.1"`

### -Message
- **类型**: String
- **默认**: "chore: 发布版本 v{版本号}"
- **说明**: Git提交消息
- **示例**: `-Message "feat: 添加图标Badge功能"`

### -ReleaseNotes
- **类型**: String
- **默认**: "Release version {版本号}"
- **说明**: GitHub Release的发布说明
- **示例**: `-ReleaseNotes "## 更新内容`n- 新功能"`

### -SkipBuild
- **类型**: Switch
- **说明**: 跳过打包步骤，仅执行Git操作
- **示例**: `-SkipBuild`

### -SkipPush
- **类型**: Switch
- **说明**: 跳过推送到GitHub，仅本地操作
- **示例**: `-SkipPush`

### -CreateRelease
- **类型**: Switch
- **说明**: 自动创建GitHub Release（需要安装GitHub CLI）
- **示例**: `-CreateRelease`

## 完整示例

### 示例1: 标准发布流程

```powershell
# 1. 更新代码
# ... 修改代码 ...

# 2. 更新 manifest.json 中的版本号（可选，脚本会自动使用）
# "version": "1.0.1"

# 3. 运行发布脚本
.\release.ps1 -Version "1.0.1" -Message "feat: 添加新功能" -CreateRelease
```

### 示例2: 仅本地发布（不推送）

```powershell
.\release.ps1 -Version "1.0.1" -SkipPush
```

### 示例3: 仅更新标签（不打包）

```powershell
.\release.ps1 -Version "1.0.1" -SkipBuild
```

### 示例4: 完整发布（包含GitHub Release）

```powershell
$notes = @"
## 版本 1.0.1 更新内容

### 新功能
- 添加了图标Badge显示功能
- 支持每5秒更新一次Badge

### 修复
- 修复了状态持久化的问题

### 改进
- 优化了性能
"@

.\release.ps1 -Version "1.0.1" -Message "feat: 添加图标Badge显示" -ReleaseNotes $notes -CreateRelease
```

## 工作流程

脚本会自动执行以下步骤：

1. **检查Git环境**
   - 验证Git是否安装
   - 检查远程仓库配置

2. **获取版本号**
   - 从 manifest.json 读取（如果未指定）
   - 或使用指定的版本号并更新 manifest.json

3. **打包**
   - 运行 build.ps1 脚本
   - 生成 zip 文件

4. **提交更改**
   - 添加所有更改到Git
   - 创建提交（使用指定的消息或默认消息）

5. **创建标签**
   - 创建 Git 标签 v{版本号}
   - 如果标签已存在，会提示是否覆盖

6. **推送到GitHub**
   - 推送代码到远程仓库
   - 推送标签到远程仓库

7. **创建GitHub Release**（可选）
   - 使用 GitHub CLI 创建 Release
   - 上传 zip 文件到 Release

## 前置要求

### 必需
- Git 已安装并配置
- PowerShell 5.0 或更高版本
- 已配置 Git 用户信息：
  ```powershell
  git config --global user.name "Your Name"
  git config --global user.email "your.email@example.com"
  ```

### 可选
- **GitHub CLI (gh)**: 用于自动创建 GitHub Release
  - 安装: https://cli.github.com/
  - 登录: `gh auth login`

## 常见问题

### Q: 如何更新版本号？

**方法1**: 手动编辑 manifest.json
```json
{
  "version": "1.0.1"
}
```

**方法2**: 使用脚本参数
```powershell
.\release.ps1 -Version "1.0.1"
```
脚本会自动更新 manifest.json 中的版本号

### Q: 如何跳过某些步骤？

```powershell
# 跳过打包
.\release.ps1 -SkipBuild

# 跳过推送
.\release.ps1 -SkipPush

# 跳过打包和推送（仅本地Git操作）
.\release.ps1 -SkipBuild -SkipPush
```

### Q: 如何创建GitHub Release？

**方法1**: 使用GitHub CLI（推荐）
```powershell
# 先安装GitHub CLI
# 然后：
.\release.ps1 -CreateRelease
```

**方法2**: 手动创建
1. 访问 GitHub 仓库
2. 点击 "Releases" → "Create a new release"
3. 选择标签并上传文件

### Q: 推送失败怎么办？

检查以下几点：
1. 网络连接是否正常
2. Git 认证是否配置正确
3. 远程仓库URL是否正确：
   ```powershell
   git remote -v
   ```

### Q: 标签已存在怎么办？

脚本会检测到已存在的标签并提示：
- 输入 `y` 删除并重新创建
- 输入 `n` 跳过标签创建

### Q: 如何自定义提交消息格式？

```powershell
.\release.ps1 -Message "feat: 添加新功能" -Version "1.0.1"
```

支持常见格式：
- `feat: 新功能`
- `fix: Bug修复`
- `docs: 文档更新`
- `chore: 发布版本`

## 最佳实践

1. **版本号管理**
   - 遵循语义化版本号（SemVer）
   - 主版本号.次版本号.修订号 (如: 1.0.0)

2. **提交消息**
   - 使用清晰的提交消息
   - 遵循约定式提交格式

3. **发布前测试**
   - 在 build 目录中测试扩展
   - 确保所有功能正常

4. **发布说明**
   - 详细描述更新内容
   - 使用 Markdown 格式
   - 包含新功能、修复、改进等

5. **定期发布**
   - 保持发布节奏
   - 及时修复重要Bug

## 故障排除

### 错误: "未检测到Git"
**解决方案**: 安装Git并确保在PATH中

### 错误: "推送失败"
**解决方案**: 
1. 检查网络连接
2. 验证Git认证（使用Personal Access Token）
3. 检查远程仓库URL

### 错误: "打包失败"
**解决方案**:
1. 检查 build.ps1 是否存在
2. 确保所有必需文件存在
3. 检查文件权限

### 错误: "GitHub CLI未安装"
**解决方案**:
- 安装 GitHub CLI: https://cli.github.com/
- 或使用 `-SkipPush` 跳过自动创建Release，手动创建

## 相关文件

- `build.ps1` - 打包脚本
- `release.ps1` - 发布脚本（本脚本）
- `manifest.json` - 扩展配置文件（包含版本号）
- `release-config.json` - 发布配置（可选）

